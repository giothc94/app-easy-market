import { ProductosClickDirective } from './productos-click.directive';

describe('ProductosClickDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductosClickDirective();
    expect(directive).toBeTruthy();
  });
});
